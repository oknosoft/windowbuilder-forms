import React from 'react';
import Typography from '@material-ui/core/Typography';

export default function SelectTemplate() {
  return <Typography>Можно перезаполнить параметры по шаблону</Typography>;
}
